$mode = "WinThreshold"
Install-WindowsFeature -Name "AD-Domain-Services" -IncludeManagementTools
Install-ADDSForest -ForestMode $mode -Domainmode $mode -DomainName "B3Care.se" -SafeModeAdministratorPassword (ConvertTo-SecureString -String "12345678.abc" -AsPlainText -Force) -InstallDns -Force
